<h1>My Articles</h1>
<p></p>

<ul class="list-inline">
  <%  @articles.each do |article| %>
    <li class="">
      <div class="d-flex justify-content-between py-1" style="width: 300px;">
        <div class="d-inline">
      <%= link_to article.title, article_path(article) %> 
      </div>
      <div class="d-inline">
      <%= link_to "✏️", edit_article_path(article), title: 'Edit this article' %> 
      <%= link_to "🗑️", article_path(article), title: 'Delete this article',method: :delete, data: { confirm: "Seguroski?"} %>
      </div>
      </div>
      </li>
  <% end %>
</ul>
<p><%= link_to 'Add a new article', new_article_path%></p>