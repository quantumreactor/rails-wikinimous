<%= simple_form_for(@article) do |a| %>
  <%= a.label :title %>
  <%= a.text_field :title %><br>
  <%= a.label :content %>
  <%= a.text_area :content, size: "80x10" %><br>
  <%= a.submit %><br>
<% end %>
